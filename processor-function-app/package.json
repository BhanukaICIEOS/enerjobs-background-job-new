{
  "name": "processor-function-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "mongoose": "^9.6.3",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/nodemailer": "^6.4.24",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
